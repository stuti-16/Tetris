import React from "react";
import { StyleStart } from "./Styles/StyleStart";

const Start = ({ callback }) => (
  <StyleStart onClick={callback}>Start Game</StyleStart>
);

export default Start;
